# Ralph Progress Log
Started: Fri Apr 10 15:19:50 CST 2026
---

## Codebase Patterns
- Dev servers: `npm run dev:server` (port 8080 WebSocket) and `npm run dev:client` (port 5173 Vite)
- Browser testing uses dev-browser CLI: `dev-browser <<'EOF' ... EOF`
- Page names persist across dev-browser calls: use "tabA" and "tabB" for two-player testing
- Canvas elements are not visible in AI snapshots - use screenshots (saveScreenshot) to verify rendering
- Player UI uses relative labels "你/对手" not absolute "玩家1/玩家2"
- App.tsx handles WebSocket state; BilliardTable.tsx handles canvas drawing and user input
- Touch events added to BilliardTable.tsx for touchpad/mobile support

## 2026-04-10 - US-020
- Verified TN-001: 建房入房与台球桌渲染
- Browser test with dev-browser using two tabs (tabA/tabB)
- Tab A: homepage showed 创建房间/加入房间 buttons ✅
- Tab A: clicked 创建房间, got 6-digit room code EFRLT0, waiting screen shown ✅
- Tab B: joined with room code EFRLT0 ✅
- Both tabs entered game interface ✅
- Tab A (Player 1): shows 你●回合 + 轮到你了 ✅
- Tab B (Player 2): shows 对手●回合 + 等待对手... ✅
- Canvas renders green billiard table, wood rails, 6 pockets (4 corner + 2 side), 16 balls in triangle break formation, white cue ball ✅
- No issues found - all acceptance criteria met
- Files changed: scripts/ralph/prd.json, scripts/ralph/progress.txt (this entry), client/src/BilliardTable.tsx (touch handlers from prior branch)
- **Learnings for future iterations:**
  - Server on port 8080 may already be running; check with `lsof -i :8080` before starting
  - Client auto-increments port if 5173/5174 are taken (5175, etc.)
  - Use `browser.getPage("tabA")` and `browser.getPage("tabB")` for persistent two-tab sessions
  - Canvas rendering must be verified via screenshots, not DOM snapshots
  - Room codes are 6 uppercase alphanumeric characters (e.g., EFRLT0)
---
