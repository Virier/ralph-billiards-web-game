## 2026-04-09 - US-011
- Implemented server-side turn timeout (60s) + client countdown + cue ball placement highlight
- Files changed:
  - server/src/index.ts (turn timer functions, Room.turnTimeoutId, auto-place on timeout)
  - client/src/types.ts (added turn_timeout to ServerMessage)
  - client/src/App.tsx (countdown state/effect, handlePlaceCueBall, countdown display in top bar)
  - client/src/BilliardTable.tsx (canPlaceCueBall prop, placement highlight drawing, click-to-place handler)
- What was implemented:
  - `startTurnTimer(room)` / `clearTurnTimer(room)` / `handleTimeout(room)`: server enforces 60s turn limit
  - Timeout handler auto-places cue ball if `canPlaceCueBall=true` at player's default position, then switches turn and sends `turn_timeout` event
  - Timer resets on each turn switch (handleTurnEnd) and after place_cue_ball (player gets 60s to shoot)
  - Client countdown: refs track turn start time; useEffect detects currentPlayer/canPlaceCueBall transitions; 500ms interval updates countdown display
  - Last 10s shown as red number for current player, orange for opponent
  - `drawPlacementHighlight()`: yellow semi-transparent zone over allowed half, dashed border, "点击放置白球" label
  - Canvas cursor changes to `cell` during placement mode
  - `handleMouseUp` dispatches to placement or aiming depending on `canPlaceCueBallRef`
- **Learnings for future iterations:**
  - setTimeout/clearTimeout stored in Room; must call clearTurnTimer in cleanupRoom and on shoot to prevent stale timers
  - Detecting React state transitions: use refs (prevCurrentPlayerRef, prevCanPlaceCueBallRef) inside useEffect to compare prev vs current values
  - `canAim = isMyTurn && !ballsMoving && !canPlaceCueBall` — placement mode disables aiming
  - Browser verification not done (no browser tools available) — manual check recommended
---

## Codebase Patterns
- Room interface has `ballsPocketedThisTurn: number[]`, `shooterThisTurn: 0|1|null`, `cueBallFirstContact: number|null`, `cueBallPocketedThisTurn: boolean` for per-shot tracking; all reset on each `shoot` event
- Turn end is detected by `wasMoved && !state.ballsMoving` in physics loop → calls `handleTurnEnd(room)`; foul detection now in handleTurnEnd (cue pocketed OR hit wrong group first)
- Group assignment: `assignGroupsIfNeeded` iterates `ballsPocketedThisTurn`, skips cue/black balls, assigns shooter = first-pocketed ball's type, opponent = opposite
- Monorepo: /client (React+Vite) and /server (Node+ws), each has own package.json and tsconfig.json
- Client typecheck: `cd client && npm run typecheck` (tsc --noEmit with bundler moduleResolution)
- Server typecheck: `cd server && npm run typecheck` (tsc --noEmit with NodeNext module)
- Server dev runner: `tsx watch src/index.ts` (no ts-node)
- Canvas rendering: BilliardTable.tsx exports TABLE_WIDTH=1400, TABLE_HEIGHT=700, BALL_RADIUS=14 — import these in future components needing coordinate space alignment
- Ball coordinate space: server ball (x,y) positions use full TABLE_WIDTH×TABLE_HEIGHT space (no rail offset); canvas renders rails as visual decoration on top
- Root package.json scripts: dev:client, dev:server, typecheck
- Server message protocol: JSON over WebSocket; client sends `join_room` (roomCode: "" to create, code to join)
- Server sends: `room_created`, `room_joined`, `error`, `start_game` (with GameState + playerIndex), `opponent_disconnected`, `game_state` (per frame), `game_over`
- Game state types (Ball, GameState) exported from server/src/index.ts for future sharing
- Client shared types live in `client/src/types.ts` (Ball, GameState, ServerMessage, ClientMessage)
- Table dimensions: 1400×700, ballRadius=14, rackX=TABLE_WIDTH*0.75 (foot spot), cueBall at x=TABLE_WIDTH*0.25
- Use `import { randomUUID } from 'crypto'` for Node.js UUID generation (not global crypto)
- Client App uses page-based state machine: 'lobby' | 'waiting' | 'game' | 'game_over'
- WebSocket is stored in useRef (wsRef) to avoid re-renders; connect only when user takes action
- Physics: Matter.js per-room engine, ballBodies Map<id, Body>, 60fps setInterval; pocket detection by distance (POCKET_RADIUS + BALL_RADIUS); walls segmented with gaps at each pocket
- TypeScript: never cast Array.indexOf() to 0|1 directly — it can return -1; check then cast
- MAX_SHOOT_POWER = 0.08 scales power (0-100%) to Matter.js force; shoot handler is in server/src/index.ts
- BilliardTable.tsx props: isMyTurn/ballsMoving/onShoot; canAim = isMyTurn && !ballsMoving guards all mouse interaction
- Use refs (canAimRef, gameStateRef, onShootRef) for stable useCallback handlers — no stale closure, no over-invalidation
- Canvas mouse→logical coords: (e.clientX - rect.left) * (TABLE_WIDTH / rect.width)
- Aiming: direction = normalize(cursor − cueBall); power = min(dist, 150) / 150 (canvas px)

# Ralph Progress Log
Started: Thu Apr  9 16:00:30 CST 2026
---

## 2026-04-09 - US-001
- Implemented full project scaffold: /client (React 18 + TypeScript + Vite) and /server (Node.js + TypeScript + ws)
- Files changed:
  - package.json (root)
  - client/package.json, client/tsconfig.json, client/vite.config.ts, client/index.html
  - client/src/main.tsx, client/src/App.tsx
  - server/package.json, server/tsconfig.json, server/src/index.ts
  - .gitignore
- **Learnings for future iterations:**
  - Client uses `moduleResolution: "bundler"` and `allowImportingTsExtensions: true` (needed for Vite)
  - Server uses `module: "NodeNext"` and `moduleResolution: "NodeNext"` for ESM-style imports
  - Use `tsx watch` for server hot reload (dev dependency: tsx)
  - node_modules were accidentally committed first time — .gitignore added afterward
---

## 2026-04-09 - US-002
- Implemented full WebSocket room management on the server
- Files changed:
  - server/src/index.ts (full rewrite with types + room logic)
- What was implemented:
  - `join_room` handler: empty roomCode → create room + 6-char code; valid roomCode → join; invalid → error response
  - Room fills at 2 players → `start_game` broadcast with initial GameState and playerIndex (0 or 1)
  - On disconnect → `opponent_disconnected` sent to remaining player, room cleaned up
  - `createInitialGameState()`: 16 balls with correct positions (triangle rack at 3/4 table, cue ball at 1/4)
  - Exported Ball and GameState interfaces for future client reuse
- **Learnings for future iterations:**
  - Use `import { randomUUID } from 'crypto'` (not `crypto.randomUUID()`) with NodeNext module system
  - `WebSocket.OPEN` check before sending prevents errors when client closes mid-message
  - `playerRoom` Map (ws → roomCode) enables O(1) room lookup on disconnect
  - Both players receive their own playerIndex (0 or 1) in `start_game` so client knows which player it is
---

## 2026-04-09 - US-003
- Implemented frontend lobby page with full WebSocket connection management
- Files changed:
  - client/src/App.tsx (full rewrite — lobby, waiting room, game stub, game over pages)
  - client/src/types.ts (new — shared types: Ball, GameState, ServerMessage, ClientMessage)
- What was implemented:
  - Lobby: "创建房间" button and "加入房间" input with 6-char code validation
  - WaitingRoom: shows room code, "等待中..." spinner, handles opponent_disconnected
  - Game page stub (canvas rendering deferred to US-004), shows playerIndex
  - Game over page with win/loss result and restart button
  - WebSocket connect-on-action pattern; cleanup on unmount via useEffect
  - All server message types handled: room_created, room_joined, start_game, game_state, game_over, opponent_disconnected, error
- **Learnings for future iterations:**
  - `React.CSSProperties` type is available without explicit React import under bundler moduleResolution (resolved from global types)
  - WS connection stored in `useRef` (not state) to avoid triggering re-renders
  - Page-state machine approach (lobby/waiting/game/game_over) maps cleanly to conditional JSX returns
  - Client types.ts mirrors server interfaces — keep in sync when server types change
  - `ws.onclose` fires after `ws.onerror`, so handle both for connection failure scenarios
---

## 2026-04-09 - US-004
- Implemented Canvas billiard table rendering
- Files changed:
  - client/src/BilliardTable.tsx (new — full Canvas component)
  - client/src/App.tsx (game page: replaced stub with BilliardTable + top bar)
- What was implemented:
  - `drawTable()`: brown rails (44px), green felt playing surface, subtle texture lines, head/foot spot dots, 6 pocket holes (4 corners + 2 mid-sides) with shadow rings
  - `drawBall()`: cue ball (radial gradient white), solid balls (1-7 with fill colour), stripe balls (9-15 white+colour band), black 8; all with specular highlight and ball number label
  - Canvas logical size 1400×700, scales responsively via CSS (width:100%, height:auto)
  - Game page top bar added showing player number and turn indicator
- **Learnings for future iterations:**
  - Canvas clip + fill trick for stripe balls: clip to ball circle, fill white base, then fillRect for colour band — stays within circle
  - For stripe ball numbers: draw a small white circle behind the number for legibility
  - ctx.save()/restore() around each ball for shadow+clip isolation
  - BilliardTable exports TABLE_WIDTH/HEIGHT/BALL_RADIUS constants — future interaction code (US-006) should import from here
  - Note: browser verification was not done (no browser tools available) — manual check recommended
---

## 2026-04-09 - US-005
- Implemented server-side Matter.js physics loop + state broadcast
- Files changed:
  - server/package.json (added matter-js dependency)
  - server/package-lock.json
  - server/src/index.ts (physics engine, per-room loop, pocket detection, broadcast)
- What was implemented:
  - `initPhysics()`: creates Matter.js Engine with gravity.scale=0, adds circle bodies for all balls and segmented wall bodies with gaps at pockets
  - `startPhysicsLoop()`: 60fps setInterval calling Engine.update(engine, 1000/60)
  - `syncPhysicsToState()`: copies body.position and body.velocity back to GameState.balls
  - `checkPocketed()`: removes ball bodies from world when center within POCKET_RADIUS+BALL_RADIUS of any pocket
  - `allBallsStopped()`: returns true when all non-pocketed ball speeds < 0.5
  - Broadcasts game_state every frame while moving, plus one final frame when stopped
  - `shoot` handler: validates turn/moving/phase, applies Body.applyForce to cue ball
  - `place_cue_ball` handler: validates bounds, recreates cue ball body at specified position
  - `stopPhysicsLoop()`: clearInterval on room teardown
- **Learnings for future iterations:**
  - Matter.js works fine in Node.js (no DOM required for physics engine)
  - `import Matter from 'matter-js'` + destructure `{ Engine, World, Bodies, Body, Events }` works with @types/matter-js
  - Wall segments with pocket gaps: `wallSegments(from, to, pocketCenters[])` helper builds segment array skipping ±POCKET_GAP around each pocket
  - TypeScript strict check: Array.indexOf() returns `number` which includes -1; cannot cast to `0|1` without a -1 check first
  - Physics per-room: Room interface extended with `physics: PhysicsState | null` holding engine + ballBodies Map + intervalId
---

## 2026-04-09 - US-006
- Implemented client-side aiming and shooting interaction
- Files changed:
  - client/src/BilliardTable.tsx (add AimState, mouse handlers, drawAimingOverlay, new Props)
  - client/src/App.tsx (add handleShoot, pass isMyTurn/ballsMoving/onShoot to BilliardTable)
- What was implemented:
  - `AimState { cursorX, cursorY, isDragging }` tracked in useState; cleared when canAim becomes false
  - `drawAimingOverlay()`: dashed ray from cue ball toward cursor (400px), ghost ball at ray end; power bar shown only while dragging
  - Power bar: bottom-center of canvas, color-coded (green/orange/red), shows "力度 N%" label
  - Cursor changes to `crosshair` when it's the player's turn and balls are stopped
  - `onMouseMove`: tracks cursor for aiming line; `onMouseDown`: sets isDragging=true; `onMouseUp`: computes dirX/dirY/power and calls onShoot
  - `onMouseLeave`: clears aim unless dragging (so user can drag outside canvas edge)
  - Ref pattern: canAimRef/gameStateRef/onShootRef used inside useCallback to avoid stale closures
  - App.tsx: `handleShoot` sends `{ type: 'shoot', dirX, dirY, power }` via WebSocket if connection open
  - isMyTurn = `gameState.currentPlayer === playerIndex`; ballsMoving = `gameState.ballsMoving`
- **Learnings for future iterations:**
  - Use stable refs inside useCallback when handler needs current prop/state — cleaner than adding everything to deps
  - setAimState with functional update (`prev =>`) works well for shoot logic — reads current aim without needing aimState in deps
  - Ghost ball at end of aiming line gives visual feedback without complex reflection logic
  - Note: browser verification was not done (no browser tools available) — manual check recommended
---

## 2026-04-09 - US-007
- Implemented server-side ball group assignment logic
- Files changed:
  - server/src/index.ts (added group assignment + turn switching + per-shot tracking)
- What was implemented:
  - `Room` extended with `ballsPocketedThisTurn: number[]` and `shooterThisTurn: 0|1|null`
  - `checkPocketed` now returns newly pocketed ball IDs each frame (order matches `state.balls` array order)
  - Physics loop accumulates pocketed IDs into `room.ballsPocketedThisTurn` each frame
  - `assignGroupsIfNeeded(state, ballsPocketed, shooter)`: iterates pocketed list, skips cue/black balls, assigns first solid/stripe ball to shooter's group and opposite to opponent
  - `handleTurnEnd(room)`: calls group assignment then switches `currentPlayer` (always-switch for now)
  - `shoot` handler resets `ballsPocketedThisTurn` and sets `shooterThisTurn = playerIndex` on each shot
  - `playerGroups` already part of `GameState` and included in every `game_state` broadcast
- **Learnings for future iterations:**
  - `checkPocketed` now returns `number[]` instead of `void` — callers must handle the return value
  - "Always switch turns" is a placeholder; US-008 should implement "keep turn if you pot your group ball without foul"
  - Group assignment only fires when `playerGroups[0] === 'unassigned'` — safe to call every turn end
  - `1 - shooter` gives opponent index cleanly when shooter is `0|1` (TypeScript requires explicit cast to `PlayerGroup`)
---

## 2026-04-09 - US-008
- Implemented server-side foul detection and ball-in-hand placement restriction
- Files changed:
  - server/src/index.ts (collision tracking, foul logic in handleTurnEnd, half-court restriction)
- What was implemented:
  - `Room` extended with `cueBallFirstContact: number|null` and `cueBallPocketedThisTurn: boolean`
  - Matter.js `Events.on(engine, 'collisionStart')` registered in `startPhysicsLoop` to track first ball-ball contact with cue ball (ignores wall contacts by checking `label.startsWith('ball_')`)
  - Physics loop: if cue ball (id=0) appears in `newlyPocketed`, sets `cueBallPocketedThisTurn = true`
  - `handleTurnEnd` foul detection: (1) cue pocketed, (2) groups assigned AND first contact was not own group
  - Foul outcome: switch turn + set `canPlaceCueBall=true` + `foulPending=true`
  - `place_cue_ball` handler: added half-court restriction — player 0 → left half (x ≤ TABLE_WIDTH/2), player 1 → right half (x ≥ TABLE_WIDTH/2)
  - `shoot` handler: blocked when `canPlaceCueBall=true`; resets `cueBallFirstContact` and `cueBallPocketedThisTurn` on each shot
  - `place_cue_ball` clears `foulPending` when placement completes
- **Learnings for future iterations:**
  - `Events.on(physics.engine, 'collisionStart', handler)` fires during Engine.update(); register once in startPhysicsLoop, closure captures room for tracking
  - Collision pairs: check `bodyA.label === 'ball_0'` or `bodyB.label === 'ball_0'`; ignore if other body label doesn't start with `ball_` (walls have label `wall`)
  - Half-court rule: player 0 = left half (x ≤ TABLE_WIDTH/2), player 1 = right half (x ≥ TABLE_WIDTH/2)
  - When `canPlaceCueBall=true`, cue ball may or may not be pocketed; place_cue_ball handler removes old body first (safe for both cases)
  - Foul tracking fields must be reset in BOTH shoot handler (start of new shot) and handleTurnEnd (after turn resolves)
---

## 2026-04-09 - US-009
- Implemented server-side win/loss detection when black 8 is pocketed
- Files changed:
  - server/src/index.ts (added broadcastGameOver function + game-over detection block in handleTurnEnd)
- What was implemented:
  - `broadcastGameOver(room, winner)`: sends `game_over` event to both players
  - In `handleTurnEnd`: checks if ball id=8 is in `ballsPocketedThisTurn`
  - Win/loss rules:
    1. Cue ball + black 8 both pocketed → shooter loses (opponent wins)
    2. Groups unassigned when 8 pocketed (e.g. on break) → shooter loses
    3. 8 pocketed but shooter still has group balls remaining → shooter loses
    4. 8 pocketed and shooter's group all cleared → shooter wins
  - On game over: sets `state.phase = 'game_over'`, `state.winner`, broadcasts `game_state` then `game_over`, stops physics loop, returns early
  - Frontend `App.tsx` already handled `game_over` events (game_over page with win/lose message and restart button was already implemented in US-003)
- **Learnings for future iterations:**
  - game-over detection must happen BEFORE resetting per-turn tracking fields (ballsPocketedThisTurn, cueBallPocketedThisTurn)
  - `stopPhysicsLoop(room)` is safe to call from within the physics loop callback (clearInterval on own intervalId)
  - `state.phase !== 'playing'` guard in shoot handler already prevents shooting after game over — no extra guard needed
  - `GameState.winner` is `number | null` in the type but the game_over event sends `0 | 1` — keep consistent
---

## 2026-04-09 - US-010
- Implemented frontend turn status UI in the game top bar
- Files changed:
  - client/src/App.tsx (replaced simple top bar with 2-panel status + center status indicator)
- What was implemented:
  - `groupLabel(group)`: returns '纯色 ①–⑦' / '花色 ⑨–⑮' / '未定'
  - `remainingBalls(gameState, group)`: counts non-pocketed balls for the given group type
  - Top bar now renders 2 player panels side-by-side, each showing: "你"/"对手" label, group name, remaining ball count
  - Active panel (current turn player) gets green border + background highlight + "● 回合" badge
  - Center status indicator shows context-aware text: place cue ball / moving / your turn / waiting for opponent
  - Panels auto-update every time `game_state` event arrives (React state update)
- **Learnings for future iterations:**
  - `[0, 1].map(pi => ...)` pattern is clean for rendering both players symmetrically
  - Spread style with conditional: `{ ...styles.base, ...(condition ? styles.active : {}) }` works in inline styles
  - `gameState.balls.filter(b => b.type === type && !b.pocketed).length` is the standard remaining-count pattern
  - Browser verification not done (no browser tools available) — manual check recommended
---
