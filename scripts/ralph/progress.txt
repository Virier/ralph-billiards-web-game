## Codebase Patterns
- Game logic is pure functions in `server/src/gameLogic.ts`; server state/physics in `server/src/index.ts`
- `evaluateTurnEnd` mutates state (calls `assignGroupsIfNeeded`) — capture pre-mutation state if needed
- Tests use vitest, run with `cd server && npm test`; typecheck with `npx tsc --noEmit` from root
- `TurnResult` interface and `evaluateTurnEnd` are the core of all turn-end rule logic
- Browser testing: server on port 8080 (WebSocket), client on port 5173 (Vite); use `dev-browser` CLI for UI verification
- `dev-browser` screenshots save to `~/.dev-browser/tmp/`; use `page.snapshotForAI()` for snapshot-based assertions

# Ralph Progress Log
Started: Fri Apr 10 10:36:44 CST 2026
---

## 2026-04-10 11:20 - US-017
- Verified lobby and room flow in browser using dev-browser skill
- Screenshots saved to ~/.dev-browser/tmp/: us017-01-homepage.png, us017-02-room-created.png, us017-03-invalid-room.png
- Verified: homepage shows "创建房间" and "加入房间" buttons (snapshot confirmed both buttons present)
- Verified: clicking "创建房间" shows 6-digit room code (e.g., O5MFZR) and "等待对手加入" waiting UI
- Verified: entering invalid room code "ZZZZZZ" shows error "房间不存在，请检查房间码"
- Files changed: scripts/ralph/prd.json (passes: true for US-017)
- **Learnings for future iterations:**
  - dev-browser `saveScreenshot` saves to `~/.dev-browser/tmp/<name>.png`
  - Server runs on port 8080 (WebSocket), client on port 5173 (Vite)
  - `page.snapshotForAI()` is reliable for verifying UI text content without screenshots
  - Use `page.goto()` to navigate back to homepage between test steps
  - Button clicks for 创建房间 need `getByRole("button", { name: "创建房间" })` (ref selectors are session-specific)
---

## 2026-04-10 10:45 - US-013
- Updated `evaluateTurnEnd` step 3 foul detection: when `cueBallFirstContact` is the 8-ball, check if shooter's group balls remain; only foul if they do
- Updated step 2 (8-ball pocketed): set `isFoul: true` when 8-ball pocketed illegally (cue ball also pocketed, or shooter still has group balls)
- Files changed: `server/src/gameLogic.ts`
- **Learnings for future iterations:**
  - Step 2 returns early when 8-ball pocketed, so step 3 foul check only runs when 8-ball is NOT pocketed
  - `state.balls` includes pocketed balls; always filter with `!b.pocketed` when checking "remaining" balls
  - Groups may change mid-function (step 1 mutates); in step 3, `state.playerGroups[shooter]` is already post-assignment
---

## 2026-04-10 10:48 - US-014
- Removed half-table restriction from `validateCueBallPlacement` (dropped `playerIndex` param)
- Updated call site in `server/src/index.ts` to use new 2-arg signature
- Updated `drawPlacementHighlight` in `client/src/BilliardTable.tsx` to highlight full table
- Updated `validateCueBallPlacement` tests to reflect full-table behavior (replaced half-table rejection tests)
- Files changed: `server/src/gameLogic.ts`, `server/src/index.ts`, `client/src/BilliardTable.tsx`, `server/src/__tests__/gameLogic.test.ts`
- **Learnings for future iterations:**
  - `validateCueBallPlacement` signature change removed `playerIndex` param — must update all call sites and tests
  - `drawPlacementHighlight` no longer needs `playerIndex`; call site updated accordingly
  - Browser verification for this story requires manual check (no dev-browser tool available in this env)
---

## 2026-04-10 10:55 - US-015
- Added `anyCushionContactThisTurn: boolean` to `Room` interface, reset on each `shoot` and `handleTimeout`
- Restructured `collisionStart` event handler to detect wall collisions (any ball with `label === 'wall'`), sets `room.anyCushionContactThisTurn = true`; cue-ball first contact tracking now runs in same loop without early-return shortcut
- Added `anyCushionContact: boolean = true` parameter (defaulted to `true` for backward-compatible tests) to `evaluateTurnEnd`
- Added step 3b in `evaluateTurnEnd`: groups assigned + no balls pocketed + `!anyCushionContact` → `isFoul = true`; break phase exempted
- Passed `room.anyCushionContactThisTurn` to `evaluateTurnEnd` in `handleTurnEnd`; reset field afterwards
- Files changed: `server/src/gameLogic.ts`, `server/src/index.ts`
- **Learnings for future iterations:**
  - Default parameter value (`= true`) on `evaluateTurnEnd` is the cleanest way to add optional params without breaking existing tests
  - The `collisionStart` early-return guard (`if (room.cueBallFirstContact !== null) return`) blocked wall detection — restructure to use per-pair checks without early-returns from the handler
  - Wall bodies have `label: 'wall'` (set in `wallOptions` in `createWalls`)
  - No-cushion foul only applies when groups are assigned AND no balls were pocketed
---

## 2026-04-10 10:50 - US-016
- Added 10 new unit tests in `server/src/__tests__/gameLogic.test.ts` covering all Chinese 8-ball rule fixes
- New describe blocks: `evaluateTurnEnd — keepTurn`, `evaluateTurnEnd — 8-ball contact legality`, `evaluateTurnEnd — no-cushion foul`
- Tests: keepTurn=true when own-group ball pocketed; keepTurn=false when only opponent ball pocketed; keepTurn=true in break phase; keepTurn=false on foul; no foul when 8-ball hit with own group cleared; foul when 8-ball hit with group remaining; no-cushion foul when groups assigned+no pocketed+no cushion; no foul with cushion contact; no no-cushion foul in break phase
- Files changed: `server/src/__tests__/gameLogic.test.ts`
- Total tests: 48 (was 38)
- **Learnings for future iterations:**
  - `validateCueBallPlacement` full-table tests were already added in US-014; no duplication needed
  - `anyCushionContact` parameter defaults to `true`, so existing tests pass without changes
  - Break phase is detected by both `wasBreakPhase` flag in the function AND by `state.playerGroups[0] === 'unassigned'` before calling evaluateTurnEnd
---

## 2026-04-10 10:40 - US-012
- Added `keepTurn: boolean` field to `TurnResult` interface
- Updated `evaluateTurnEnd` to compute `keepTurn`: true when no foul and own-group ball pocketed (or break phase with any non-cue/non-black ball pocketed)
- Updated `handleTurnEnd` in `index.ts` to only switch `currentPlayer` when `!result.keepTurn`
- Files changed: `server/src/gameLogic.ts`, `server/src/index.ts`
- **Learnings for future iterations:**
  - `assignGroupsIfNeeded` mutates state early in `evaluateTurnEnd`; must save `wasBreakPhase` flag BEFORE calling it
  - The 40 existing tests don't check `keepTurn`, so adding a new return field is backward compatible
  - Foul always implies `keepTurn = false`; game over always implies `keepTurn = false`
---
